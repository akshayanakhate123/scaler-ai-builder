# Secrets — never commit real keys
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.env

# Scraper / runtime cache
# NOTE: scraped_cache.json IS committed as a grounding baseline for deploy
# (public Scaler marketing text, no secrets). The deployed app refreshes it live
# when it can and falls back to the committed snapshot otherwise.
.scaler_cache/
*.log

# Assignment brief — keep the private brief out of the public repo
*.pdf

# OS / editor
.DS_Store
Thumbs.db
