"""Pre-call nudge generation (BDA-facing, no approval gate).

STUB — build order step 3. Not implemented in step 1.
See SPEC.md sections 6a and 9.
"""


def generate_nudge(profile: str) -> dict:
    """TODO(step 3): call llm.gemini_generate() with prompts.NUDGE_* and return a
    dict of the nudge sections. For now this is a placeholder."""
    raise NotImplementedError("generate_nudge is a step-3 stub — not built yet.")
